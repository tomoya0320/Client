using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Battle {
  public class EffectManager : BattleBase {
    public EffectManager(BattleManager battleManager) : base(battleManager) { 
    
    }
  }
}