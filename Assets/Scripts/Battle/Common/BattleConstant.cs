using System;

namespace Battle {
  public static class BattleConstant {
    public const int THOUSAND = 1000;
    public static readonly int ATTRIB_LENGTH = Enum.GetValues(typeof(AttribType)).Length;
  }
}